[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
markers =
    unit: Unit tests without external infrastructure
    integration: Integration tests with real infrastructure
    api: API tests over FastAPI endpoints
    e2e: End-to-end user journeys across bounded contexts
